# This is a basic workflow to help you get started with Actions

name: Faze.Core

# Controls when the action will run. Triggers the workflow on push or pull request
# events but only for the master branch
on:
  workflow_dispatch:
  push:
    branches: [ master ]
    paths: [ 'src/libraries/Faze.Core/Faze.Core/Faze.Core.csproj' ]

env:
  SOLUTION_PATH: 'src/libraries/Faze.Core'

# A workflow run is made up of one or more jobs that can run sequentially or in parallel
jobs:
  # This workflow contains a single job called "build"
  build:
    runs-on: ubuntu-latest
      
    steps:
    - uses: actions/checkout@v2
    - name: Setup .NET Core SDK ${{ matrix.dotnet-version }}
      uses: actions/setup-dotnet@v1.7.2
      with:
        dotnet-version: '5.0.x'
    - name: Install dependencies
      run: dotnet restore "${{ env.SOLUTION_PATH }}"
    - name: Build
      run: dotnet build "${{ env.SOLUTION_PATH }}" --configuration Release --no-restore
    - name: Test
      run: dotnet test "${{ env.SOLUTION_PATH }}" --no-restore --verbosity normal --collect:"XPlat Code Coverage" -- DataCollectionRunSettings.DataCollectors.DataCollector.Configuration.Format=lcov
    - name: move-coverage
      run: find "${{ env.SOLUTION_PATH }}/Faze.Core.Tests/TestResults/" -type f -print0 | xargs -0 mv -t "${{ env.SOLUTION_PATH }}/Faze.Core.Tests/TestResults"
    # You may pin to the exact commit or the version.
    # uses: coverallsapp/github-action@9ba913c152ae4be1327bfb9085dc806cedb44057
    - name: Coveralls GitHub Action
      uses: coverallsapp/github-action@1.1.3
      with:
        github-token: ${{ secrets.GITHUB_TOKEN }}
        # Path to lcov file
        path-to-lcov: '${{ env.SOLUTION_PATH }}/Faze.Core.Tests/TestResults/coverage.info'
        # Job flag name, e.g. "Unit", "Functional", or "Integration". Will be shown in the Coveralls UI.
        # The root folder of the project that originally ran the tests
        #base-path: '${{ env.SOLUTION_PATH }}'
  
 
  publish-nuget:
    needs: build
  
    # The type of runner that the job will run on
    runs-on: ubuntu-latest

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      - uses: actions/checkout@v2

      - name: Publish NuGet
        # You may pin to the exact commit or the version.
        # uses: brandedoutcast/publish-nuget@c12b8546b67672ee38ac87bea491ac94a587f7cc
        uses: brandedoutcast/publish-nuget@v2.5.5
        with:
          # Filepath of the project to be packaged, relative to root of repository
          PROJECT_FILE_PATH: '${{ env.SOLUTION_PATH }}/Faze.Core/Faze.Core.csproj'
          NUGET_KEY: ${{secrets.NUGET_KEY}}
  
  publish-github:
    needs: build
  
    # The type of runner that the job will run on
    runs-on: ubuntu-latest

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
    - uses: actions/checkout@v2
    - name: Setup .NET Core @ Latest
      uses: actions/setup-dotnet@v1
      with:
        source-url: https://nuget.pkg.github.com/b-faze/index.json
      env:
        NUGET_AUTH_TOKEN: ${{secrets.GITHUB_TOKEN}}

    - name: Build solution and generate NuGet package
      run: dotnet build "${{ env.SOLUTION_PATH }}/Faze.Core" -c Release

    - name: Push generated package to GitHub registry
      run: dotnet nuget push "${{ env.SOLUTION_PATH }}/Faze.Core/bin/Release/*.nupkg" --skip-duplicate --no-symbols true
